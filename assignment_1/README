
Use tcpdump to capture the messages:
 tcpdump -nnqXs0 tcp and port 10001 and 'tcp[13] != 2 and tcp[13] != 16'

Connecting and controlling the program:
 nc localhost 10002

Commands:
 - j[b|IP Address] : Join node, b is for bootstrap.
 - q : Quit
 - s : Search